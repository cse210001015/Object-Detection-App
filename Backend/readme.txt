For tfjs you need to select the install additional plugins for node while installing node.
Then just do npm install.

For gm:Install the gm converters and add it to path. Restart your pc and do npm install.
For text in gm: Install ghostscript and the ghostscript fonts seperately. Put the fonts inside the ghostscript folder and add its
bin to the path. Now restart your pc.